# ─────────────────────────────────────────────────────────────
# Spoke Quote Builder — environment variables
# Copy to .env.local for local dev. Set the same names in Vercel
# (preview AND production) before promoting. Never commit real values.
# ─────────────────────────────────────────────────────────────

# Supabase project jdvksxdfiquzilijavhh
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Server-only. Never prefix with NEXT_PUBLIC_. Never expose to the browser.
SUPABASE_SERVICE_ROLE_KEY=

# GitHub backup (daily product/quote backup)
GITHUB_BACKUP_TOKEN=

# Google Sheets pricing sync
GOOGLE_SHEETS_ID=
GOOGLE_SERVICE_ACCOUNT_EMAIL=
GOOGLE_SERVICE_ACCOUNT_PRIVATE_KEY=
